{% extends '::layout.html.twig' %}

{% block title %}
    Workspace
{% endblock %}

{% block submenu %}
    <nav class="navbar navbar-default" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <span class="navbar-brand">{{ workspace.name }}</span>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                     <li><a href="{{ path('pm_workspace_todo', {'id': workspace.id })}}">Mes tâches</a></li>
                     <li><a href="{{ path('pm_userroleworkspace_index', {'workspace_id': workspace.id })}}">Membres</a></li>
                     <li><a href="{{ path('pm_task_index', {'workspace_id': workspace.id })}}">Tâches</a></li>
                     <li><a href="{{ path('pm_workflow_edit', {'id': workspace.id})}}">Workflow</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
    
{% block content %}{% endblock %}